# 听译记 0.2.0

听译记是一款面向英文线下课堂的桌面实时翻译与笔记工具。它在本机完成英文语音识别，将稳定字幕交给用户自己配置的 DeepSeek 服务翻译和整理，并在课程结束后生成可保存的 Markdown 课堂笔记。

## 面向使用者

发布给普通使用者时，只提供构建完成的安装包：

- Apple 芯片 Mac：`听译记-macOS-AppleSilicon.dmg`
- Windows：后续由 Windows 独立打包流程生成，不要把开发项目目录直接发给使用者。

使用者不需要看到源代码、测试、模型缓存、数据库或开发截图。首次打开后，只需要在“设置”里粘贴自己的 DeepSeek API Key，并选择笔记输出目录。

## 隐私说明

- 英文麦克风音频在支持的本地模式下由电脑本机识别。
- DeepSeek 会收到用于翻译、实时摘要和最终整理的文字内容。
- 课程数据库和导出笔记默认保存在使用者自己的电脑中。
- 发布包绝不能包含开发者的 `.env`、课程数据库、课堂录音、导出笔记、日志或本地模型缓存。

## macOS 构建

在 Apple 芯片 Mac 上解压“macOS 构建包”，依次运行：

1. `安装听译记-macOS.command`
2. `构建听译记-macOS.command`

完成后，把 `dist/听译记-macOS-AppleSilicon.dmg` 上传给使用者。使用者打开后，将“听译记”拖到同一窗口的 Applications 文件夹即可。当前为未签名个人版本；正式公开分发前应使用 Apple Developer ID 签名并完成 notarization。
